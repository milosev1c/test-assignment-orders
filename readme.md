## How to run

Any of these variants:

- With docker run by running `docker build -t <any tag> .` and `docker run <any tag>:latest` 
- With docker compose by running `docker compose up --build`
- With venv (or any environment with python) after installing packages
  from `requirements.txt` (`python manage.py runserver 0.0.0.0:8000`)